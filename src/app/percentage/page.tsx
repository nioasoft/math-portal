import PercentageClient from '@/components/worksheet/PercentageClient';

export const metadata = { title: 'דפי עבודה - אחוזים' };

export default function PercentagePage() {
    return <PercentageClient />;
}
